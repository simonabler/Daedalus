# =============================================================================
# Daedalus — Environment Configuration
# =============================================================================
#
# Provider routing is determined by the model string:
#
#   OpenAI   →  any model name that doesn't match the patterns below
#               e.g.  CODER_1_MODEL=gpt-4o
#
#   Anthropic →  any model name containing "claude"
#               e.g.  CODER_1_MODEL=claude-opus-4-5
#
#   Ollama   →  model name prefixed with "ollama:"
#               e.g.  CODER_1_MODEL=ollama:llama3.1:70b
#               Requires: pip install langchain-ollama
#               Requires: Ollama running locally (see OLLAMA_BASE_URL)
#
# You only need API keys for the providers you actually use.
# =============================================================================

# --- API Keys (only required for the providers you use) ---------------------
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# --- Ollama (for local models) ----------------------------------------------
# URL of your local Ollama server. Default: http://localhost:11434
# OLLAMA_BASE_URL=http://localhost:11434

# --- Agent Models -----------------------------------------------------------
# Planner coordinates the overall task plan
PLANNER_MODEL=gpt-4o-mini

# Coder 1 — implements even-numbered items, reviews Coder 2's odd-numbered items
# Examples:
#   CODER_1_MODEL=gpt-4o
#   CODER_1_MODEL=claude-opus-4-5
#   CODER_1_MODEL=ollama:llama3.1:70b
CODER_1_MODEL=gpt-4o-mini

# Coder 2 — implements odd-numbered items, reviews Coder 1's even-numbered items
# Examples:
#   CODER_2_MODEL=gpt-4o
#   CODER_2_MODEL=claude-sonnet-4-20250514
#   CODER_2_MODEL=ollama:deepseek-coder-v2
CODER_2_MODEL=gpt-4o-mini

# Supporting agents
DOCUMENTER_MODEL=gpt-4o-mini
TESTER_MODEL=gpt-4o-mini

# --- Target Repository ------------------------------------------------------
TARGET_REPO_PATH=

# --- Telegram Bot (optional) ------------------------------------------------
TELEGRAM_BOT_TOKEN=
TELEGRAM_ALLOWED_USER_IDS=

# --- Web UI -----------------------------------------------------------------
# WEB_HOST=127.0.0.1
WEB_PORT=8420

# --- Behaviour --------------------------------------------------------------
MAX_ITERATIONS_PER_ITEM=5
MAX_REWORK_CYCLES_PER_ITEM=3
SHELL_TIMEOUT_SECONDS=120
LOG_LEVEL=INFO
